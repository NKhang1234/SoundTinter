version: 1.0

extractor:
  frameSize: 2048
  hopSize: 1024

lowlevel:
  enabled: false   # <- 🚫 disables the whole section

rhythm:
  enabled: true
  descriptors:
    bpm:
      stats: [mean]

tonal:
  enabled: true
  descriptors:
    key:
      stats: [mean]
